[project]
name = "rag"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10, <4.0.0"
dependencies = [
    "langchain>=1.2.8",
    "langgraph>=1.0.7",
    "sqlalchemy>=2.0.46",
]

[dependency-groups]
dev = [
    "mypy>=1.10,<2.0",
    "pre-commit>=3.7",
]

[tool.mypy]
strict = true
